import intro from "./intro"
import call from "./call"

var data = {
    id: "divergence",
    name: "Divergence",
    season: 3,
    pages: [
        intro,
        call
    ]
};

export default data;